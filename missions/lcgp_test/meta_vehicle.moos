ServerHost = localhost
ServerPort = $(VPORT)

// Community name IS the vehicle name
Community  = $(VNAME)

#include plug_origin_warp.moos

//------------------------------------------------------------
ProcessConfig = ANTLER
{
  MSBetweenLaunches = 100

  Run = MOOSDB             @ NewConsole = false
  Run = pLogger            @ NewConsole = false
  Run = pRealm             @ NewConsole = false
  Run = uSimMarine         @ NewConsole = false
  Run = pNodeReporter      @ NewConsole = false
  Run = pMarinePID         @ NewConsole = false
  Run = pHelmIvP           @ NewConsole = false
  Run = uProcessWatch      @ NewConsole = false
  Run = pShare             @ NewConsole = false
  Run = pHostInfo          @ NewConsole = false
  Run = uFldNodeBroker     @ NewConsole = false
  Run = uFldMessageHandler @ NewConsole = false
  Run = pTrajToWaypoints   @ NewConsole = false
  Run = pObstacleMgr       @ NewConsole = false

  Run = pContactMgrV20     @ NewConsole = false

  Run = pCommsMgr          @ NewConsole = false
  Run = pLocalize          @ NewConsole = false
  Run = pAvoidAgents       @ NewConsole = false
  Run = pAvoidObstacles    @ NewConsole = false
  Run = pEOptimality       @ NewConsole = false
}

#include plug_uFldNodeBroker.moos
#include plug_pShare.moos
#include plug_pHostInfo.moos
#include plug_uFldMessageHandler.moos

#include plug_uSimMarine.moos
#include plug_pHelmIvP.moos
#include plug_pMarinePID.moos
#include plug_pLogger.moos
#include plug_pNodeReporter.moos
#include plug_uProcessWatch.moos
#include plug_pTrajToWaypoints.moos
#include plug_pObstacleMgr.moos

#include plug_pContactMgrV20.moos

#include plug_pCommsMgr.moos
#include plug_pLocalize.moos
#include plug_pAvoidAgents.moos
#include plug_pAvoidObstacles.moos
#include plug_pEOptimality.moos